{
  "name": "reactbricks-starters",
  "private": true,
  "scripts": {
    "start": "turbo run start",
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "check-types": "turbo run check-types",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "enter-changeset-alpha": "changeset pre enter alpha",
    "create-changeset": "changeset",
    "changeset-version": "changeset version",
    "changeset-publish": "changeset publish",
    "exit-changeset-alpha": "changeset pre exit"
  },
  "devDependencies": {
    "prettier": "^3.6.0",
    "prettier-plugin-tailwindcss": "^0.6.11",
    "turbo": "^2.5.6"
  },
  "packageManager": "pnpm@10.16.1",
  "engines": {
    "node": ">=20"
  },
  "prettier": {
    "printWidth": 80,
    "semi": false,
    "singleQuote": true,
    "trailingComma": "es5"
  },
  "dependencies": {
    "@changesets/cli": "^2.29.7"
  }
}